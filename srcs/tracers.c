#include "minirt.h"


int trace_shapes(t_ray ray, t_shapes shapes, t_p3d *phit)
{
    int color;
    int i;

    color = azure;
    i = 0;
    while (i < shapes.top)
    {

        i++;
        // intersect_shape()//
    }
    return(color);
}